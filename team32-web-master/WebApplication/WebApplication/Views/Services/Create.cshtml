@model Service
@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="row">
    <div class="col-sm-9 col-md-7 col-lg-4 mx-auto">
        <section id="loginForm">
            <div class="card card-signin my-5">
                <div class="card-body">
                    <h2 class="text-center my-2">@ViewBag.Title</h2>
                    @using (Html.BeginForm("Create", "Services", FormMethod.Post, new { @class = "form-signin effect-3", role = "form" }))
                    {
                        @Html.AntiForgeryToken()
                        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                        <div class="form-group">
                            <div class="form-label-group my-2">
                                @Html.LabelFor(m => m.Name, new { @class = "text" })
                                @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
                            </div>
                            <p class="text text-danger">@Html.ValidationMessageFor(m => m.Name, "", new { @class = "text text-danger" })</p>
                        </div>
                        <div class="form-group">
                            <div class="form-label-group my-2">
                                @Html.LabelFor(m => m.Description, new { @class = "text" })
                                @Html.TextAreaFor(m => m.Description, new { @class = "form-control" })
                            </div>
                            <p class="text text-danger">@Html.ValidationMessageFor(m => m.Description, "", new { @class = "text-danger" })</p>
                        </div>
                        <p class="text-center text-@ViewBag.Style">@ViewBag.Message</p>
                        <div class="form-group">
                            <div class="form-label-group my-2">
                                <input type="submit" value="Create Service" class="btn btn-primary form-control" />
                            </div>
                        </div>
                    }
                </div>
            </div>
        </section>
    </div>
</div>

