@using WebApplicationv2.Models.Account;

@model LoginModel

@{
    ViewData["Title"] = "Login";
    Layout = "~/Views/Shared/_AccountLayout.cshtml";
}

<div class="row">
    <div class="col-sm-9 col-md-7 col-lg-4 mx-auto">
        <section id="loginForm">
            <div class="card card-signin my-5">
                <div class="card-body">
                    <h2 class="text-center my-2">@ViewBag.Title</h2>
                    @using (Html.BeginForm("Login", "Account", FormMethod.Post, new { @class = "form-signin effect-3", role = "form" }))
                    {
                        @Html.AntiForgeryToken()
                        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                        <div class="form-group">
                            <div class="form-label-group my-2">
                                @Html.LabelFor(m => m.Email, new { @class = "text" })
                                @Html.TextBoxFor(m => m.Email, new { @class = "form-control" })
                            </div>
                            <p class="text text-danger">@Html.ValidationMessageFor(m => m.Email, "", new { @class = "text text-danger" })</p>
                        </div>
                        <div class="form-group">
                            <div class="form-label-group my-2">
                                @Html.LabelFor(m => m.Password, new { @class = "text" })
                                @Html.PasswordFor(m => m.Password, new { @class = "form-control" })
                            </div>
                            <p class="text text-danger">@Html.ValidationMessageFor(m => m.Password, "", new { @class = "text-danger" })</p>
                        </div>
                        <div class="form-group">
                            <div class="form-label-group my-2">
                                <div class="checkbox">
                                    @Html.CheckBoxFor(m => m.RememberMe)
                                    @Html.LabelFor(m => m.RememberMe)
                                </div>
                            </div>
                        </div>
                        <p class="text-center text-@ViewBag.Style">@ViewBag.Message</p>
                        <div class="form-group">
                            <div class="form-label-group my-2">
                                <input type="submit" value="Login" class="btn btn-primary form-control" />
                            </div>
                        </div>
                        <p class="text-center">
                            @Html.ActionLink("Register as a new user", "Register")
                        </p>
                        <p class="text-center">
                            @Html.ActionLink("Forgot your password?", "ForgotPassword")
                        </p>
                    }
                </div>
            </div>
        </section>
    </div>
</div>

