<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="22" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="22" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="iQueue"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".BranchesMapsActivity"
            android:label="Select a Branch"></activity>
        <activity
            android:name=".RateBranch"
            android:label="Rate a Branch" />
        <activity
            android:name=".HomeP"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Update_Profile"
            android:label="Update Profile" />
        <activity android:name=".BA" />
        <activity android:name=".QueueItem" />
        <activity android:name=".QueueList" />
        <activity
            android:name=".Testing"
            android:label="@string/title_activity_testing" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity
            android:name=".MapsActivitytest"
            android:label="@string/title_activity_maps_activitytest" />
        <activity
            android:name=".MapsLocation"
            android:label="Branches" />
        <activity
            android:name=".QueueInfo"
            android:label="Queue Status" />
        <activity
            android:name=".Login"
            android:label="Login" />
        <activity
            android:name=".QueueToken"
            android:label="My Queue Token" />
        <activity
            android:name=".Register"
            android:label="Register" />
        <activity
            android:name=".queuestatus"
            android:label="Current Queue Status" />
        <activity
            android:name=".BActivity"
            android:label="Select a Branch" />
        <activity android:name=".QueueInformation" />
        <activity
            android:name=".ServiceList"
            android:label="Select a Service" />
        <activity android:name=".MainActivity" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />
    </application>

</manifest>